<% comment_name = 'comment_' + @comment_id %>
<% if @destroyed %>
  $("<%= '#' + comment_name %>").remove();
<% else %>
  $("<%= '#' + comment_name + '_error_message' %>").html('<%= @comment.errors.first[1] %>');
<% end %>
