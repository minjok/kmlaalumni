<% posting = @comment.posting %>
<% posting_name = 'posting_' + posting.id.to_s %> 
$("<%= '#' + posting_name + '_get_more_comments_link_container' %>").html('<%= escape_javascript(render partial:'postings/posting_get_more_comments_link', locals:{posting:posting}) %>')
$("<%= '#' + 'comment_' + @comment.id.to_s %>").remove();
