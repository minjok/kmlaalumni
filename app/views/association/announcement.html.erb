<div id='header'>
  <div class='header_title'>동문회 공지사항</div>
</div>
<% if current_user.is_admin %>
  <%= render partial: 'postings/new_posting_form', locals: {value: Posting::PLATFORM['ANNOUNCEMENT'], resource: Posting.new, placeholder: current_user.name + ' 동문님, 여기에 동문회 공지사항을 올려주세요'} %>
<% end %>
<div id='page_content_container'>
  <div id="postings">
  </div>
</div>
<script type='text/javascript'>
  var page = 1;
  var numPages = 0;
  var loading = true;
  var content = 'postings'
  var params = <%= ("'platform=announcement'").html_safe %>

  $(document).ready( feed() );
</script>
